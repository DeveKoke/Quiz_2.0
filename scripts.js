let arr [];
let randomNum = Math.floor(Math.random()*20);
if(!arr.includes(randomNum)){
    arr.push(randomNum)
}else{
    return randomNum
}
console.log(randomNum);

for (let i = 0; arr.index(); i++) {
    const element = array[index];
    
}